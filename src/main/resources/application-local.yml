spring:
  datasource:
    url: jdbc:mysql://localhost:3306/control_panel_db?useSSL=false&serverTimezone=UTC
    username: root
    password: toor
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate.jdbc.time_zone: UTC
      hibernate.format_sql: true
    open-in-view: false

server:
  port: 8099

logging:
  level:
    esvar.ua.dekanatcontrolpanel: DEBUG
    root: INFO
